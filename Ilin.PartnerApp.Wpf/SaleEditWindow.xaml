<Window x:Class="Ilin.PartnerApp.Wpf.SaleEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ilin.PartnerApp.Wpf"
        mc:Ignorable="d"
        Title="SaleEditWindow" Height="530" Width="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Padding="15"
                BorderBrush="#D0D0D0"
                BorderThickness="1"
                CornerRadius="6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="170"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="12"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="12"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="12"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="12"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="12"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="12"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="Партнер:"/>
                <ComboBox x:Name="PartnerComboBox"
                          Grid.Row="0"
                          Grid.Column="1"
                          Height="30"
                          DisplayMemberPath="CompanyName"
                          SelectedValuePath="Id"
                          SelectionChanged="InputControl_Changed"
                          VerticalContentAlignment="Center"/>

                <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Text="Товар:"/>
                <ComboBox x:Name="ProductComboBox"
                          Grid.Row="2"
                          Grid.Column="1"
                          Height="30"
                          DisplayMemberPath="Name"
                          SelectedValuePath="Id"
                          SelectionChanged="InputControl_Changed"
                          VerticalContentAlignment="Center"/>

                <TextBlock Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Text="Количество:"/>
                <TextBox x:Name="QuantityTextBox"
                         Grid.Row="4"
                         Grid.Column="1"
                         Height="30"
                         TextChanged="InputControl_Changed"
                         VerticalContentAlignment="Center"/>

                <TextBlock Grid.Row="6" Grid.Column="0" VerticalAlignment="Center" Text="Дата продажи:"/>
                <DatePicker x:Name="SaleDatePicker"
                            Grid.Row="6"
                            Grid.Column="1"
                            Height="30"
                            VerticalContentAlignment="Center"/>

                <TextBlock Grid.Row="8" Grid.Column="0" VerticalAlignment="Center" Text="Комментарий:"/>
                <TextBox x:Name="CommentTextBox"
                         Grid.Row="8"
                         Grid.Column="1"
                         Height="60"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"/>

                <Border Grid.Row="10"
                        Grid.Column="0"
                        Grid.ColumnSpan="2"
                        Margin="0,10,0,0"
                        Padding="12"
                        BorderBrush="#D8D8D8"
                        BorderThickness="1"
                        CornerRadius="6"
                        Background="#FAFAFA">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Базовая цена:" FontWeight="SemiBold"/>
                            <TextBlock x:Name="BasePriceTextBlock" Margin="0,4,0,0" Text="0.00"/>

                            <TextBlock Margin="0,10,0,0" Text="Скидка:" FontWeight="SemiBold"/>
                            <TextBlock x:Name="DiscountTextBlock" Margin="0,4,0,0" Text="0%"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Цена продажи:" FontWeight="SemiBold"/>
                            <TextBlock x:Name="UnitPriceTextBlock" Margin="0,4,0,0" Text="0.00"/>

                            <TextBlock Margin="0,10,0,0" Text="Итоговая сумма:" FontWeight="SemiBold"/>
                            <TextBlock x:Name="TotalAmountTextBlock" Margin="0,4,0,0" Text="0.00"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <Border Grid.Row="2"
                Padding="10"
                BorderBrush="#D0D0D0"
                BorderThickness="1"
                CornerRadius="6">
            <DockPanel>
                <TextBlock x:Name="StatusTextBlock"
                           DockPanel.Dock="Left"
                           VerticalAlignment="Center"
                           Foreground="DimGray"
                           Text=""/>

                <StackPanel DockPanel.Dock="Right"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <Button x:Name="SaveButton"
                            Width="120"
                            Height="35"
                            Margin="0,0,10,0"
                            Content="Сохранить"
                            Click="SaveButton_Click"/>

                    <Button x:Name="CancelButton"
                            Width="120"
                            Height="35"
                            Content="Отмена"
                            Click="CancelButton_Click"/>
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</Window>
